# Minimal .env for Docker build
# Actual values are injected by Railway at runtime
APP_NAME="LeanScale Platform"
APP_ENV=production
APP_KEY=base64:placeholder-key-replaced-at-runtime=
APP_DEBUG=false
APP_URL=https://localhost
LOG_CHANNEL=stack
DB_CONNECTION=pgsql
CACHE_DRIVER=database
QUEUE_CONNECTION=database
SESSION_DRIVER=database
